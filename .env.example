# ================================================================
# CodePlayground - Local Development Environment Configuration
# ================================================================
# INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Update passwords and secrets below
# 3. Run: docker-compose up -d
# ================================================================

# Database Configuration (Docker Compose)
POSTGRES_DB=codeplayground
POSTGRES_USER=codeplayground_admin
POSTGRES_PASSWORD=local_dev_password_123  # Change for production

# Backend Configuration
SPRING_PROFILES_ACTIVE=dev
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/codeplayground
SPRING_DATASOURCE_USERNAME=codeplayground_admin
SPRING_DATASOURCE_PASSWORD=local_dev_password_123  # Must match POSTGRES_PASSWORD

# Frontend Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
NEXT_PUBLIC_APP_NAME=CodePlayground

# JWT Configuration
JWT_SECRET=your-local-development-jwt-secret-key-here  # Generate secure key for production

# AWS Configuration (for deployment only)
AWS_REGION=ap-northeast-2
# AWS credentials are configured via `aws configure` command

# Application Settings
LOG_LEVEL=DEBUG
NODE_ENV=development
CODE_EXECUTION_TIMEOUT=30000
MAX_CODE_LENGTH=10240

# ================================================================
# SECURITY NOTES:
# - These are LOCAL development settings
# - NEVER use these passwords in production
# - Generate secure random passwords for production deployment
# - Use AWS Secrets Manager for production secrets
# ================================================================